name: Hello World

run-name: Print Hello World

on:
    push:
        branches:
            - master
jobs:
    hello_world_job:
        runs-on: ubuntu-20.04
        steps:
            - name: say hello
              run: |
                 echo 'Hello, world!'